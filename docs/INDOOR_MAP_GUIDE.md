# Руководство по интерактивной indoor-карте

## Обзор

Новая система интерактивной карты для EasyShop предоставляет современный user-friendly интерфейс для навигации внутри помещений, аналогичный Google Maps или Яндекс.Картам.

## Основные возможности

### 1. **Интерактивные маркеры**
- 🏪 **Магазины** - красочные градиентные маркеры с иконками
- 🚪 **Вход** - зеленый маркер с иконкой двери
- 🚶 **Выход** - красный маркер с иконкой человека
- 📍 **Ваше местоположение** - пульсирующий синий маркер

### 2. **Анимированные маршруты**
- Плавные линии с анимацией "бегущего пунктира"
- Автоматическое центрирование на маршруте
- Информационная панель с расстоянием, временем и количеством точек
- Стрелки направления вдоль маршрута

### 3. **Поиск магазинов**
- Автокомплит при вводе 2+ символов
- Поиск по названию магазина и категории
- Отображение до 5 релевантных результатов
- Мгновенная реакция на выбор

### 4. **Управление картой**
- **Zoom In (+)** - приблизить карту
- **Zoom Out (−)** - отдалить карту
- **Reset (⊡)** - показать всю карту целиком
- Поддержка touch-жестов (pinch-to-zoom)

### 5. **Интерактивность**
- Hover-эффекты на маркерах (масштабирование, подсветка)
- Всплывающие подсказки (tooltips) с названиями
- Popup-окна с деталями при клике на магазин
- Кнопка "Построить маршрут" в popup

## Архитектура

### Структура компонентов

```
Map/
├── Map.js                      # Главный контейнер карты
├── MapImage/
│   ├── SetupMap.js            # Инициализация карты
│   ├── MapImage.js            # Координатор слоев
│   ├── CategoryRouteGeneration.js  # Построение маршрутов
│   ├── CustomMarker.js        # Кастомные маркеры
│   ├── AnimatedRoute.js       # Анимация маршрутов
│   ├── PathfindingService.js  # A* алгоритм pathfinding
│   └── InteractiveLayer.js    # Интерактивные слои
└── MapControls/
    ├── SearchControl.js       # Поиск магазинов
    └── ZoomControl.js         # Управление масштабом
```

### Используемые библиотеки

- **Leaflet** v1.9.3 - базовая картографическая библиотека
- **React-Leaflet** v4.1.0 - React-интеграция для Leaflet
- **pathfinding** v0.4.18 - A* алгоритм для построения маршрутов
- **@turf/turf** v7.3.3 - геопространственный анализ
- **leaflet-geometryutil** v0.10.3 - утилиты для работы с геометрией
- **leaflet.markercluster** v1.5.3 - кластеризация маркеров (для будущего)

## Использование

### Базовая интеграция

```jsx
import Map from './components/Shop/Map/Map';

function ShopPage() {
    return (
        <Map
            categories={categories}
            isBuildRouteClicked={false}
            source={null}
            destination={null}
            postBuildRoute={() => {}}
            height={620}
        />
    );
}
```

### Построение маршрута

```jsx
const [buildRoute, setBuildRoute] = useState(false);

<Map
    categories={categories}
    isBuildRouteClicked={buildRoute}
    source={1}  // ID начальной точки
    destination={5}  // ID конечной точки
    postBuildRoute={() => setBuildRoute(false)}
    height={620}
/>
```

### Кастомизация маркеров

```javascript
import { CustomMarker } from './MapImage/CustomMarker';

// Создать маркер магазина
const marker = CustomMarker.createShopMarker(
    [y, x],                    // Координаты
    'Название магазина',        // Название
    'Продукты'                  // Категория
);

// Создать маркер входа
const entrance = CustomMarker.createEntranceMarker([y, x]);

// Создать маркер "Вы здесь"
const location = CustomMarker.createCurrentLocationMarker([y, x]);
```

### Работа с PathfindingService

```javascript
import { PathfindingService } from './MapImage/PathfindingService';

// Инициализация
const pathfinding = new PathfindingService(1653, 993);

// Определить проходимые зоны
pathfinding.initializeGrid([
    { x: 100, y: 100, width: 200, height: 300 },  // Коридор
    { x: 400, y: 200, width: 150, height: 150 }   // Комната
]);

// Найти путь
const path = pathfinding.findPath([y1, x1], [y2, x2]);

// Сгладить путь
const smoothPath = pathfinding.smoothPath(path);

// Вычислить расстояние
const distance = pathfinding.calculateDistance(path);
```

### Анимированный маршрут

```javascript
import { AnimatedRoute } from './MapImage/AnimatedRoute';

const animatedRoute = new AnimatedRoute(map);

// Нарисовать маршрут
animatedRoute.drawAnimatedRoute(
    pathPoints,
    startMarker,
    endMarker
);

// Добавить путевые точки
animatedRoute.highlightWaypoints([
    [y1, x1],
    [y2, x2],
    [y3, x3]
]);

// Очистить маршрут
animatedRoute.clearRoute();
```

## Стилизация

Все стили находятся в `assets/styles/map.css`. Основные CSS-классы:

- `.custom-shop-marker` - стили маркеров магазинов
- `.animated-route` - анимированная линия маршрута
- `.room-tooltip` - всплывающие подсказки
- `.route-info-panel` - информационная панель маршрута
- `.map-search-container` - контейнер поиска
- `.custom-zoom-controls` - кнопки масштабирования

### Кастомизация цветов

```css
/* Изменить цвет маршрута */
.animated-route {
    stroke: #4285F4;  /* Google Blue */
}

/* Изменить градиент маркеров */
.custom-shop-marker {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

## API Endpoints

### Построение маршрута

```
GET /api/build-route/{source}/{destination}

Response:
[
    {
        "id": 1,
        "x_coordinate": 100,
        "y_coordinate": 200,
        "category": { "title": "Продукты" }
    },
    ...
]
```

## Производительность

### Оптимизация для большого количества маркеров

Если на карте более 100 маркеров, рекомендуется использовать кластеризацию:

```javascript
import 'leaflet.markercluster';
import 'leaflet.markercluster/dist/MarkerCluster.css';

const markers = L.markerClusterGroup({
    maxClusterRadius: 50,
    spiderfyOnMaxZoom: true
});

categories.forEach(cat => {
    const marker = CustomMarker.createShopMarker(...);
    markers.addLayer(marker);
});

map.addLayer(markers);
```

### Ленивая загрузка маршрутов

Для оптимизации используется индикатор загрузки во время построения маршрута:

```javascript
// Показать индикатор загрузки
const loadingDiv = L.DomUtil.create('div', 'route-loading');
loadingDiv.innerHTML = `
    <div class="spinner"></div>
    <span>Построение маршрута...</span>
`;
map.getContainer().appendChild(loadingDiv);
```

## Будущие улучшения

### Планируется добавить:

1. **Многоэтажность** - переключение между этажами здания
2. **Голосовая навигация** - пошаговые инструкции
3. **AR-режим** - дополненная реальность через камеру
4. **Offline-режим** - кеширование карты для работы без интернета
5. **Тепловые карты** - визуализация загруженности зон
6. **Расписание работы** - интеграция с графиком работы магазинов

### Как преобразовать вашу схему в интерактивную карту

1. **Структурируйте SVG** - добавьте `id` и `data-` атрибуты к элементам
2. **Создайте GeoJSON** - конвертируйте SVG в GeoJSON с метаданными
3. **Определите проходимые зоны** - отметьте коридоры и доступные области
4. **Настройте граф** - создайте узлы и связи для pathfinding
5. **Добавьте метаданные** - названия, категории, часы работы

Пример структурированного SVG:

```xml
<rect 
    id="shop-23" 
    data-type="shop" 
    data-name="Продуктовый" 
    data-category="food"
    x="164" y="168" width="112" height="36"
/>
```

## Поддержка и отладка

### Включение debug-режима

```javascript
// В SetupMap.js
map.on('click', (e) => {
    console.log('Clicked coordinates:', e.latlng);
});

// В PathfindingService.js
console.log('Walkable at', x, y, ':', this.isWalkable(x, y));
```

### Типичные проблемы

**Проблема**: Маршрут не строится
- Проверьте, что обе точки находятся в проходимой зоне
- Убедитесь, что grid инициализирован
- Проверьте координаты (должны быть [y, x], не [x, y])

**Проблема**: Маркеры не отображаются
- Убедитесь, что CSS подключен (`import './styles/map.css'`)
- Проверьте консоль на ошибки загрузки иконок
- Убедитесь, что координаты в правильном формате

**Проблема**: Анимация не работает
- Проверьте, что CSS-класс `.animated-route` применен
- Убедитесь, что браузер поддерживает CSS animations
- Проверьте z-index слоев

## Контакты

Для вопросов и предложений по улучшению карты обращайтесь к разработчикам проекта EasyShop.

---

**Версия**: 1.0.0  
**Последнее обновление**: 29 января 2026
