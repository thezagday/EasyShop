1. Сейчас когда я хочу зайти в http://admin.localhost/ меня редиректит на http://localhost/, а такого быть не должно. админ - это админ, туда могут заходить только юзеры с ролью ROLE_ADMIN
2. в http://localhost/ могут заходить все, но туда так же можно и зарегестрироваться и войти. После регистрации надо придумать роль, возможно ROLE_USER ? и таким образом чтобы два хоста: http://admin.localhost/ и http://localhost/ были разделены по ролям.
3. плюс мне не очень нравится что общие UI компоненты определяют выводить ли какой либо элемент уже на самой станице в зависимости от хоста и роли, может есть более удачный способ ? например вот это:
    const isAdmin = roles.includes('ROLE_ADMIN');
    const adminUrl = rootElement ? rootElement.getAttribute('data-admin-url') : '/admin';
    const isAdminHost = window.location.hostname.includes('admin.');
выглядит не очень красиво. 
4. Почему в компоненте App.js есть компоненты которые не используются, как например Contact ? они просто скрыты на фронте ? если да то покажи где и как и возможно лушче просто не добавлять их в App.js.

Сейчас мы добавили возможность для залогиненных юзеров добавлять: контекст, смотреть историю, создавать свои подборки и смотреть свою статистику.
1. Чтобы AIAssistant понял юзеровский контекст, то мы на фронте его считываем вот так:   const userId = rootElement ? rootElement.getAttribute('data-user-id') : null;, а можем ли мы это делать на бэке ? если юзер залогинен, то мы знаем от кого идет запрос и на бэке из объекта юзера сможем достать контекст. 
2. При просмотре истории мы видим только запросы от юзера, но не видим сам маршрут на карте, который был. Можем ли это тоже выводить как то ?
3. вопрос большой с подборками: у нас есть возможность сохранять подборки, но там есть много проблем с фронтом. надо чтобы юзер создавал подборку под определенный магазин, и соответсвтенно должно быть поле, в котором будет указан магазин подборки, и поле с товарами, примерно как это в админке.
4. Все еще статистика не работает корректно, там нчего не выводится.